HOEOS G6.1 — VERIFICATION PERSISTENCE SCHEMA & RLS EVIDENCE
============================================================

1. TIMESTAMP
Timestamp: 2026-09-12T07:09:10+01:00

2. MIGRATION CREATED
- Path: supabase/migrations/20260912_public_verification_records.sql
- Description: DDL table creation, lookup indexes, and RLS policies for public verification authority.

3. EXACT TABLE CREATED
- Table: public.public_verification_records

4. EXACT COLUMNS & TYPES
- id (UUID PRIMARY KEY DEFAULT gen_random_uuid())
- verification_id (TEXT NOT NULL UNIQUE)
- document_hash (TEXT NOT NULL)
- evidence_hash (TEXT NOT NULL)
- engine_version (TEXT NOT NULL DEFAULT 'G5-DETERMINISTIC-v1')
- policy_version (TEXT NOT NULL DEFAULT 'G3-POLICY-v1')
- overall_similarity (NUMERIC NOT NULL CHECK (overall_similarity >= 0 AND overall_similarity <= 100))
- total_sources_found (INTEGER NOT NULL DEFAULT 0)
- evidence_envelope (JSONB NOT NULL)
- created_at (TIMESTAMPTZ NOT NULL DEFAULT NOW())

5. CONSTRAINTS & INDEXES
- Primary Key: id (UUID)
- Uniqueness: verification_id (TEXT UNIQUE)
- Check Constraint: overall_similarity >= 0 AND overall_similarity <= 100
- Index: idx_public_verification_records_verification_id (UNIQUE INDEX ON verification_id)
- Index: idx_public_verification_records_document_hash (INDEX ON document_hash)
- Index: idx_public_verification_records_evidence_hash (INDEX ON evidence_hash)
- Index: idx_public_verification_records_created_at (INDEX ON created_at DESC)

6. RLS POLICIES & SECURITY BOUNDARIES
- RLS Enabled: ALTER TABLE public.public_verification_records ENABLE ROW LEVEL SECURITY;
- Select Policy: "Public Verification Record Select Policy"
  * Action: FOR SELECT
  * Target Roles: anon, authenticated
  * Expression: USING (true)
  * Purpose: Enables anonymous verifiers to resolve public verification receipts via /verify/{verificationId}.
- Insert Policy: "Authenticated Verification Record Insert Policy"
  * Action: FOR INSERT
  * Target Roles: authenticated, service_role
  * Purpose: Restricts verification record creation strictly to authenticated runners / service endpoints.
- Anonymous Write Denial:
  * Anonymous INSERT: DENIED (No policy granted to anon)
  * Anonymous UPDATE: DENIED (Default RLS deny, policy omitted)
  * Anonymous DELETE: DENIED (Default RLS deny, policy omitted)

7. AUTHORITY BOUNDARY MECHANISM
- In G6.2, writes to public_verification_records will be owned by authenticated server/service-role API endpoints. The client browser running the frontend UI cannot overwrite or tamper with document_hash, evidence_hash, overall_similarity, or evidence_envelope.

8. TESTS EXECUTED & COUNTS
- Schema & RLS Test Suite (tests/verificationSchemaRLS.test.ts): 20 / 20 PASSED (0 FAILED)
- QR & Determinism Suite (tests/qrGenerator.test.ts): 22 / 22 PASSED
- VerifyCore Suite (tests/verifyCore.test.ts): 27 / 27 PASSED
- Bulk Ingestion Suite (tests/bulkIngestionAndMetadata.test.ts): 32 / 32 PASSED
- Plagiarism Policy Suite (tests/plagiarismEvidencePolicy.test.ts): 48 / 48 PASSED
- AI Firewall & Matrix Suite (tests/aiEvidenceFirewall.test.ts): 24 / 24 PASSED
- Canonical Document Suite (tests/canonicalDocumentContract.test.ts): 25 / 25 PASSED
- Deterministic Evidence Bridge Suite (tests/deterministicEvidenceBridge.test.ts): 17 / 17 PASSED
- Total Verify Subsystem Test Count: 255 / 255 PASSED (0 FAILED)

9. ENVIRONMENT LIMITATIONS
- ENVIRONMENT LIMITATION: Live Supabase anon key is unavailable in local .env.local (VITE_SUPABASE_ANON_KEY not configured). Static DDL, column definitions, lookup indexes, and RLS policy boundaries were verified deterministically against the migration contract.

10. FINAL CERTIFICATION STATUS
Certification: G6.1 CERTIFIED PASS
